This is the console app - server side of a RF Frequency Simulator app. It allows TCP socket transmission of random Freq and Db data via UDP. User can set ip address. 
To run the matching front end, clone RF Simulator as well, which is Python / Html, Flask and JS based.
