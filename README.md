This is the c++ console app - server side of associated 'simple RF Frequency Simulator app'. It allows socket transmission of random Freq and Db data via UDP. 
To run the matching front end, clone RF Simulator as well, which is Python / Html, Flask and JS based.
